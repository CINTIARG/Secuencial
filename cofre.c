#include <stdio.h>
#include <math.h>
int main (){
    int a,b,c,x,y,z,aux;
    scanf("%d %d %d %d %d %d",&a,&b,&c,&x,&y,&z);
    if (a,b,c,x,y,z>=1 && a,b,c,x,y,z<=1000){ aux=(x*y*z)-(a*b*c);}
    if (aux>=5) {printf("1\n");} else {printf("0\n"); }
    system("pause");
    return 0;
}
